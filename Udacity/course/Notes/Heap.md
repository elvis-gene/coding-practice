A heap is represented with an array. You place elements from left to right and each level (from top to bottom) having twice as many elements than the previous level.

Not every arrays can be represented as a heap. 

The array saves space by not using pointers nodes use.
